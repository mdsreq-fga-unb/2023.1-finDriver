import React from "react";
import { View, Image, Text, Pressable, StyleSheet } from 'react-native';

const UserSettings = ({navigation}) => {
    return(
        <View>
            <Text>PRECISAMOS DECIDIR ISSO AQUI TAMBEM</Text>
        </View>
    )
}

export default UserSettings;